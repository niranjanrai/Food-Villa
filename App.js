const div = React.createElement("h1", {}, "hello world");
const root = React.createRoot(document.getElementById("root"));
root.render(div);

/**
 * Bundlers : vite, webpack, parcel
 * Parcel
 *
 *
 *
 *
 */
